// external deps
import { StyleSheet } from 'react-native';

export const DrawerStyles = StyleSheet.create({

    titleList: {
        flexDirection: 'row',
        flexWrap: 'wrap'
    },
    titleBtn: {
        margin: 5,
        padding: 5,
        borderWidth: 1,
        borderColor: 'black',
        borderRadius: 1,
        alignItems: 'center'
    }
});
